// filename: c1-1.h. The storing structre is order and adopting dynamic allocation.
typedef ElemType *Triplet; // The space of three elements is allocated by InitTriplet function. The type of triplet is a pointer which point into ElemType type, it stores the address of ElemType type.
